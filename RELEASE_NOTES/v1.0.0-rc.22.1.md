# v1.0.0-rc.22.1 (2025-12-06)

Mobile optimization patch for the Neo Easter Egg introduced in RC22.

### Fixed

- **Mobile Display**: Morpheus message now fully visible on iPhone Pro Max and other mobile devices
  - Responsive message box with `max-height: 85vh` and scrollable overflow
  - Reduced padding from 50px to 30px for better space utilization
  - Smaller font size (15px vs 18px) and tighter line-height (1.6 vs 1.8) on mobile
  - Smooth iOS scrolling with `-webkit-overflow-scrolling: touch`
  - Green Matrix-themed scrollbar styling
  - Ensures complete message including "— Morpheus" signature is visible

### Technical Details

- Detects mobile viewport (`window.innerWidth <= 768`)
- Applies responsive styling automatically
- Box is now scrollable when content exceeds viewport height
- Native iOS momentum scrolling for better UX

### Notes

Pure frontend fix. Recommended to test on actual mobile devices (iPhone Pro Max, etc.) to verify full message visibility.
